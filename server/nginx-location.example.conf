# Add inside the HTTPS server block for aiwords.top.
location /multidoc-sync-api/ {
    limit_req zone=multidoc_sync burst=20 nodelay;
    proxy_pass http://127.0.0.1:8792/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
    access_log off;
}

# Add once inside the http block:
# limit_req_zone $binary_remote_addr zone=multidoc_sync:10m rate=30r/m;
