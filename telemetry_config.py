# Public ingestion endpoint. This is intentionally not a secret; desktop apps
# cannot safely hide client-side keys. Leave empty until the self-hosted API is
# deployed, then set the HTTPS URL before building a release.
TELEMETRY_ENDPOINT = ""
